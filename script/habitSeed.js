const moment = require('moment')

// Store date of creation
let newDate = new Date()
console.log(newDate)
newDate = newDate - 3600 * 24
console.log(newDate)
console.log(moment.utc(newDate))
const now = 'hello'
console.log(now)
// find all weeks (Monday to Sunday) up to todays date.

// associate a dollar value with those weeks

// store information in some way
